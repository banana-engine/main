// import: local interfaces
import { Vector2 } from '../types/Vector2.js'

// declaration
/**
 * Object to describe camera location, rotation, etc.
 */
export interface Camera {
    position: Vector2
    rotation: number
}