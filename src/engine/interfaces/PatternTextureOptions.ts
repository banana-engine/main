// import: local types
import { PatternRepeatMode } from 'engine/types/PatternRepeatMode.js'

// declaration
/**
 * Options for `RepeatingTexture`.
 */
export interface PatternTextureOptions {
    path: string
    repeat: PatternRepeatMode
}